{
  "name": "citeskill",
  "version": "0.1.0",
  "description": "Transparent citations for agent-assisted repositories",
  "type": "module",
  "bin": { "citeskill": "./dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/*.test.js",
    "check": "npm run test"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
